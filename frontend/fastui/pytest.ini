# pytest configuration for FastUI frontend tests
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --strict-markers
    --tb=short
markers =
    e2e: marks tests as end-to-end tests
    unit: marks tests as unit tests
    smoke: marks tests as smoke tests